export class SR4EItem extends Item {
  prepareBaseData() {
    super.prepareBaseData();
  }

  prepareDerivedData() {
    super.prepareDerivedData();
    // TODO: normalize modifiers, requirements, effects, and mounted components.
  }
}
