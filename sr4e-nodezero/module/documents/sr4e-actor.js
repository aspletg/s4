export class SR4EActor extends Actor {
  prepareBaseData() {
    super.prepareBaseData();
  }

  prepareDerivedData() {
    super.prepareDerivedData();
    // TODO: derive attributes, condition tracks, initiative, armor, matrix stats.
  }
}
